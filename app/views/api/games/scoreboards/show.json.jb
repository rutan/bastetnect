# frozen_string_literal: true

jsend_success(
  data: {
    scoreboard: {
      name: @scoreboard.name,
      rank_order: @scoreboard.rank_order,
      index: @scoreboard.index,
      is_overwrite_best_score: @scoreboard.is_overwrite_best_score
    }
  }
)
