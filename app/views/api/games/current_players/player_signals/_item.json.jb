# frozen_string_literal: true

{
  id: player_signal.id,
  sender: render(partial: 'api/games/players/item', locals: { player: player_signal.sender }),
  data: player_signal.data,
  created_at: player_signal.created_at.to_i
}
