# frozen_string_literal: true

{
  id: game_signal.id,
  sender: render(partial: 'api/games/players/item_summary', locals: { player: game_signal.sender }),
  data: game_signal.data,
  createdAt: game_signal.created_at.to_i
}
